import { createTheme } from '@mui/material'

export const DefaultTheme = createTheme({
    palette: {
        background: {
            default: 'black',
        },
        text: {
            primary: '#adb1c9',
            secondary: '#adb1c9',
        },
    },
})
